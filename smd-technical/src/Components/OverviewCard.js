import {React, useState} from "react";


export function overviewCard(props){

const title = props.title;
const number= props.number;
const socMed = props.socMed;
const increment = props.increment;

    return (
        <div>

            
        </div>
    );
}